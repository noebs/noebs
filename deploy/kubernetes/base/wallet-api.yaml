apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-api
  annotations:
    argocd.argoproj.io/sync-wave: "20"
  labels:
    app.kubernetes.io/name: wallet-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: wallet-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wallet-api
    spec:
      serviceAccountName: wallet-api
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: wallet-api
          image: ghcr.io/noebs/noebs:master
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            exec:
              command: [/usr/local/bin/noebs, internal-healthcheck]
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            exec:
              command: [/usr/local/bin/noebs, internal-healthcheck]
            periodSeconds: 30
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          volumeMounts:
            - name: config
              mountPath: /app/config.yaml
              subPath: config.yaml
              readOnly: true
            - name: config
              mountPath: /app/service.yaml
              subPath: wallet-api.service.yaml
              readOnly: true
            - name: wallet-api-secrets
              mountPath: /app/secrets.yaml
              subPath: secrets.yaml
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: noebs-config
        - name: wallet-api-secrets
          secret:
            secretName: wallet-api-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: wallet-api
  labels:
    app.kubernetes.io/name: wallet-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wallet-api
  ports:
    - name: http
      port: 8080
      targetPort: http
