The synthetic quote fixture was obtained from the deployed pinned SDK v24.19.5 cache after its live HTTP POST /transfers returned 200 and WAITING_FOR_QUOTE_ACCEPTANCE on 10 September 2026 UTC. The cache's internal `quoteReceived` state is replaced with that observed HTTP response state; all native party/quote envelopes and their header value types are preserved. This includes numeric Content-Length values that handwritten fixtures omitted. No transfer was admitted or prepared for this quote. Tests use a fixed time before its original expiry. The source runtime digest is e84dd7c6ff535f21cd24056b2547cc14c5fd8ea7834d2fe681d4c1879ac844d2. The fixture predates explicit resource-version pinning and records the SDK's 2.0 request headers and the existing participant's 1.1 responses.
